# Claude Instructions

Follow `AGENTS.md` for repository structure, commands, validation, and safety rules.

Before changing code, read the relevant files. Keep edits minimal and scoped to the request. Never reveal or commit values from `.env`.
